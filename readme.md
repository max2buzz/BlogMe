1.Install node.js (MongoDB and memcached are hosted on aws, so no installation required)

2.To run the project, from project directory execute following command

npm start

3.Go to http://localhost:3000 from browser